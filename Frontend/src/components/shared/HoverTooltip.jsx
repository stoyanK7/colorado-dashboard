import ReactTooltip from 'react-tooltip';

const HoverTooltip = () => {
  return (
    <ReactTooltip
      backgroundColor='var(--carnelian)'
      textColor='var(--white)'
    />
  );
};

export default HoverTooltip;